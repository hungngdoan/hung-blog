<header class="site-header">
  <h1 class="site-title">{{ site.title }}</h1>
  <p class="site-subtitle">{{ site.subtitle | safe }}</p>
  <div class="banner-wrap">
    <img
      src="{{ site.bannerSrc }}"
      alt="{{ site.bannerAlt }}"
    />
  </div>
</header>
