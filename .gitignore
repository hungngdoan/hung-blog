engineer_principle_local.md
.claude/
node_modules/
_site/

# Published audio (.opus) is committed and deployed. MP3s are local working
# files only (licensing unverified, see README "Music and NCS tracks") -- never publish.
src/music/*.mp3

# Local working files (image sources, prototypes, scratch) -- never published
assets-work/
.tmp-*.patch
movies.sqlite
.pytest_cache/
.ruff_cache/
